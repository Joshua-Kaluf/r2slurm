% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submit.R
\name{write_slurm_script}
\alias{write_slurm_script}
\title{Write a Slurm submission script to disk}
\usage{
write_slurm_script(job, path)
}
\arguments{
\item{job}{A \code{slurm_job} object.}

\item{path}{File path where the script should be written.}
}
\value{
Invisibly returns the path to the written script.
}
\description{
Converts a \code{slurm_job} object into a bash script and writes
it to the specified path. Automatically creates directories if needed.
}
\examples{
job <- slurm_job("echo Hello", job_name = "hello")
tmpfile <- tempfile(fileext = ".sh")
write_slurm_script(job, tmpfile)
cat(readLines(tmpfile), sep = "\n")

}
